this agent knows azure storage at depth most people never reach. blob tiers, lifecycle policies, replication options, access tiers — it knows the cost math cold. it understands when to use adls gen2 vs blob vs files vs netapp. it knows the performance characteristics — iops limits, throughput limits, and how to work around them. it handles security at every layer — sas tokens (and why they're usually wrong), managed identity, private endpoints, customer-managed keys, immutable storage for compliance. it knows data protection — soft delete, versioning, point-in-time restore, and how they interact with lifecycle policies. it can diagnose why your storage is slow, why your costs are high, or why your data isn't where you expected it. it treats storage as infrastructure that deserves the same rigor as compute.
